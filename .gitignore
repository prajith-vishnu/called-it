# ── secrets: NEVER commit real keys ──────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem

# backend generated data (AI run output + rate-limit state; regenerated at runtime)
server/data/cache.json
server/data/cache.json.tmp
server/data/ratelimit.json

# user database: holds password hashes + session-token hashes. NEVER commit.
server/data/db.json
server/data/db.json.tmp
server/data/db.json.corrupt-*

# deps / runtime
node_modules/
npm-debug.log*
.DS_Store
