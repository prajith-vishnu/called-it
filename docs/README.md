# docs/ — README media

Real screenshots of the running app (headless Chrome, actual UI, not mockups):

- `feed.png` — the feed, crowd-belief bars, category tabs
- `locked-in.png` — a card right after picking an option
- `celebration.png` — the "beat the crowd" reveal, mid-confetti
- `banner.png` — composed from the two screenshots above, used at the top of the main README and as the hackathon submission banner

If you record a real device instead, keep the same filenames so the README links keep working. Optional: add `demo.gif` (5-10s of tap → locked in → confetti → points update) and un-comment its line in the main README.
