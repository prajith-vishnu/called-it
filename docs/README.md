# docs/ — README media

App preview images used by the main README (`feed.png`, `locked-in.png`,
`celebration.png`). They're renders of the app's actual UI; feel free to swap
any of them for device screenshots — keep the same filenames so the README
links keep working.

Optional: add a `demo.gif` (5–10s of tap → locked in → confetti → points
update, under ~5 MB) and un-comment its line in the main README.

Capture tips: open the app, DevTools device toolbar at ~390×844, then
"Capture screenshot" — or record with QuickTime/Kap for the GIF.
