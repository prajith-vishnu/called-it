{
  "name": "called-it-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Zero-dependency backend for the Called It prediction game: accounts (scrypt + httpOnly sessions), server-scored picks + cached leaderboard, daily streaks, and a scheduled, triple-rate-limited Groq refresh that only ever enriches a cache. No packages = nothing for npm audit to flag.",
  "type": "commonjs",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "start:no-env-file": "node server.js",
    "refresh": "node --env-file=.env scripts/refresh-cli.js",
    "refresh:force": "node --env-file=.env scripts/refresh-cli.js --force"
  },
  "dependencies": {}
}
